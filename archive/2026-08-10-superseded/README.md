# Superseded material

These files are retained for lineage and are not deployed:

- `lateral-notes/`: the extracted stacked-notes interface;
- `notes/`: its construction note;
- `animation-frames*.txt`: animation inspection output;
- `claude-skills/`: project-local BMAD tooling that is not needed to build the site.

The current site lives at the repository root on `main`.
